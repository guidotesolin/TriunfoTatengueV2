// Replace with the data fetched from https://www.emailjs.com/

const getConfig = () => {
  const config = {
    serviceID: "",
    templateID: "",
    userID: "",
  };
  return config;
};

export default getConfig;
